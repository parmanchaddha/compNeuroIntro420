    # println(weights)

    # for i in range(1, weightSize)
    #     for j in range(1, weightSize)
    #         if !isclose(weights[i,j], after1IterationWeights[i,j])
    #             println(i, j, weights[i,j], after1IterationWeights[i,j])
    #         end
    #     end

    # end